name: weather_app
description: "A beautiful weather application with real-time forecasts, animated weather icons, and a modern neumorphic UI design."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  # Core
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  
  # State Management 
  # provider: ^6.1.1
  
  # Local Storage 
  # shared_preferences: ^2.2.2
  
  # Location 
  # geolocator: ^10.1.0
  # permission_handler: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # Uncomment if you add assets
  # assets:
  #   - assets/images/
  #   - assets/icons/
